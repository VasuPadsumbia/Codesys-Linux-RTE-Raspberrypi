[Unit]
# Wait for network to be fully online before starting chronyd.
# Without this, chrony starts before routes/DNS are ready, fails its initial
# server contact, and then backs off for up to 1024 s before retrying.
After=network-online.target
Wants=network-online.target
