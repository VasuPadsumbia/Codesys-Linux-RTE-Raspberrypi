# systemd system.conf.d — confine all non-RT services to CPU0 and CPU1
# CPU2 and CPU3 are reserved for EtherCAT (SCHED_FIFO 90) and CODESYS (SCHED_FIFO 80).
# Individual services with CPUAffinity= set explicitly override this default.

[Manager]
CPUAffinity=0 1
