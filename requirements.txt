# CCLRTE Python requirements
# All tools run inside venv/ — created automatically by ./cclrte.sh
# Install manually: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# Build system
kas>=4.0

# Testing
pytest>=7.4
pytest-cov>=4.1
requests>=2.31

# WebUI (target-side, installed via Yocto recipes — listed here for local dev/test)
flask>=3.0
werkzeug>=3.0

# QEMU test helpers
pexpect>=4.9
